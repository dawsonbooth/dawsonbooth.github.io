extensions:
  languageService:
    cacheSchemaFileForLookup: true
projects:
  projects:
    schema:
      - https://api.github.com/graphql:
        headers:
          Authorization: Bearer ${VITE_GITHUB_TOKEN}
    documents: src/projects/api/**/*.{graphql,tsx,jsx,ts,js}
